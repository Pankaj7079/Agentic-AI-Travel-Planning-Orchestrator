[project]
name = "parikrama-common"
version = "0.1.0"
description = "Shared constants, enums, and utilities for PariKrama"
requires-python = ">=3.12"

dependencies = [
    "pydantic>=2.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/parikrama_common"]
