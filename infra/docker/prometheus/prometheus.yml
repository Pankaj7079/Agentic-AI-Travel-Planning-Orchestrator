# Prometheus configuration for PariKrama
global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "parikrama-backend"
    static_configs:
      - targets: ["host.docker.internal:8000"]
    metrics_path: /metrics
    scrape_interval: 10s
